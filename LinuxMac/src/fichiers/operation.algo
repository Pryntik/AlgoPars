ALGORITHME Opération
constante:
cons : entier
variables:
    val : entier
DEBUT
    écrire ( "valeur initiale :")
    lire ( val )
    val <-- val + 2
    val <-- val × 2
    tq val < 9 alors
        val <-- val + 2
    ftq
    val <-- val / 2
    val <-- val - 1
    écrire ( val )

FIN